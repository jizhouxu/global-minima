# Optional: public HTTPS form endpoint accepting POST fields `name` and `email`.
# This URL is rendered into HTML: never put a private API key here.
# Leave unset to disable email signup fields and display the coming-soon message.
# NEWSLETTER_ACTION=https://your-provider.example/subscribe
